name: Validate

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Validate repository
        env:
          PYTHONUTF8: "1"
        run: python3 scripts/validate_repo.py

      - name: Build release archive
        run: python3 scripts/package_skill.py

      - name: Verify deterministic rebuild
        run: |
          python3 scripts/package_skill.py --output /tmp/zh-complex-project-orchestrator-rebuild.zip
          cmp --silent "dist/zh-complex-project-orchestrator-$(cat VERSION).zip" /tmp/zh-complex-project-orchestrator-rebuild.zip

      - name: Test release archive
        run: unzip -t "dist/zh-complex-project-orchestrator-$(cat VERSION).zip"
